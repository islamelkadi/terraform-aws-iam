# IAM Policy Example

Basic example creating an IAM policy with DynamoDB and S3 permissions, attached to a role.

## Usage

```bash
terraform init
terraform plan -var-file=params/input.tfvars
terraform apply -var-file=params/input.tfvars
```

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
